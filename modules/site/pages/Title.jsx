import styled from "styled-components";

export default () => {
  const Wrapper = styled.h1`
    padding-top: 2em;
    font-size: 3em;
  `;

  return <Wrapper>PHP isn't going away anytime soon.</Wrapper>;
};
